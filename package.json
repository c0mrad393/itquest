{
  "name": "triageos",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev -p 3000",
    "build": "next build",
    "//build:check": "A production build that does NOT touch .next, so it can run while `npm run dev` is up. `next build` and `next dev` share .next and clobber each other — a build mid-session leaves the running dev server throwing `Cannot find module './NNN.js'` until it is restarted.",
    "build:check": "NEXT_DIST_DIR=.next-build next build",
    "start": "next start -p 3000",
    "lint": "next lint",
    "typecheck": "tsc --noEmit",
    "test:physics": "rm -rf .test-build tsconfig.test.tsbuildinfo && tsc -p tsconfig.test.json && node tests/prepare.mjs && node tests/rack-physics.mjs",
    "test": "npm run typecheck && npm run test:physics"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.106.2",
    "next": "^14.2.35",
    "react": "18.3.1",
    "react-dom": "18.3.1",
    "zustand": "^4.5.4"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "autoprefixer": "^10.4.19",
    "postcss": "^8.4.39",
    "tailwindcss": "^3.4.7",
    "typescript": "^5.5.4"
  }
}
